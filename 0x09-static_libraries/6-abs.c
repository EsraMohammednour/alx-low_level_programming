#include "main.h"

/**
 * _abs - Function to calcute abslute value for integer number
 *
 * @x: Take intger number for funcin
 *
 * Description: Calculate absluute value
 *
 * Return: Always 0 (Success)
 */

int _abs(int x)
{
	if (x < 0)
		x  = (-1) * x;
		return (x);
}
